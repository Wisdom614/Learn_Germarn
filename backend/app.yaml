runtime: python311
entrypoint: uvicorn main:app --host 0.0.0.0 --port $PORT

env_variables:
  PYTHONUNBUFFERED: "1"

instance_class: F1
automatic_scaling:
  target_cpu_utilization: 0.65
  max_instances: 10
